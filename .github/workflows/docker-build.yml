name: Build and Push

on:
  push:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=sha
          type=raw,value=latest

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # - name: Register to distributor
    #   run: |
    #     SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
    #     IMAGE_NAME_LOWER=$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
    #     curl -X POST https://distributor.app238.com/api/register \
    #       -H "Content-Type: application/json" \
    #       -d "{\"worker_id\": \"exhibitor\",\"owner_id\": \"jw238\",\"image\": \"${{ env.REGISTRY }}/${IMAGE_NAME_LOWER}:sha-${SHORT_SHA}\",\"port\": 10086}"

    - name: Set short SHA
      run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV

    - name: Deploy to Combinator
      uses: Jabberwocky238/combinator-action@main
      with:
        secret-key: sk_8DxOH16wKyAt0OpsSWerIw1e216Ji-MJ
        user-id: ivory7195299
        worker-id: exhibitor
        image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ env.SHORT_SHA }}
        port: 10086